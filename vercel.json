{
    "version": 2,
    "builds": [
      {
        "src": "src/api/server.ts",
        "use": "@vercel/node",
        "config": {
          "includeFiles": ["server.json"]
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "src/api/server.ts"
      }
    ]
  }