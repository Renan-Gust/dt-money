{
    "version": 2,
    "builds": [
      {
        "src": "api/server.ts",
        "use": "@vercel/node",
        "config": {
          "includeFiles": ["server.json"]
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.ts"
      }
    ]
  }